type Dimensions = readonly [number, number];
let dimensions: Dimensions = [1920, 1080];

// dimensions.push(5);

console.log(
  `The dimensions of the screen are ${dimensions[0]}x${dimensions[1]}`
);

let dimensions2: number[] = [1920, 1080];

console.log(dimensions2);
